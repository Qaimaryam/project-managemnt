name: Build and Test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compile Program
        run: g++ -o test test1.cpp
      - name: Run Tests
        run: ./test
