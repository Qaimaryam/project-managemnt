name: Build and Test
on: [push]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Compile Program
        run: g++ -o test main.cpp
      - name: Run Tests
        run: ./test.exe
